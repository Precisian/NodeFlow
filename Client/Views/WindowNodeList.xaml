<Window x:Class="Client.Views.WindowNodeList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client.Views"
        mc:Ignorable="d"
        Title="WindowNodeList" Height="270" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- GroupBox를 첫 번째 행에 배치합니다. -->
        <GroupBox Header="노드 목록" Grid.Row="0">
            <Grid Margin="10,10,10,10">
                <!-- 내부 Grid의 행을 정의합니다. 각 필드가 하나의 행을 차지합니다. -->
                <Grid.RowDefinitions>
                    <RowDefinition Height="6*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DataGrid ItemsSource="{Binding Properties}" 
                  SelectedItem="{Binding SelectedProperty}"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  Grid.Row="0" SelectionChanged="DataGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="선택" Binding="{Binding IsSelected}"/>
                        <DataGridTextColumn Header="#" Binding="{Binding Name}" Width="0.25*"/>
                        <DataGridTextColumn Header="이름" Binding="{Binding Name}" Width="1.5*"/>
                        <DataGridTextColumn Header="갯수(Src)" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="갯수(Tgt)" Binding="{Binding Name}" Width="*"/>    
                    </DataGrid.Columns>
                </DataGrid>

                <!-- '추가' 버튼을 두 번째 행에 배치합니다. -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1">
                    <Button Content="취소" Margin="5" VerticalAlignment="Center" HorizontalAlignment="Right" Width="60" Command="{Binding ClickedCancel}"/>
                    <Button Content="삭제" Margin="5" VerticalAlignment="Center" HorizontalAlignment="Right" Width="60" Command="{Binding ClickedDelete}"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</Window>
